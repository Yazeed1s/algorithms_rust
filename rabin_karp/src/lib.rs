fn search(pattern: String, text: String, q: usize) {
    let m = pattern.len();
    let n = text.len();
}

#[cfg(test)]
mod tests {
    #[test]
    fn it_works() {
        let result = 2 + 2;
        assert_eq!(result, 4);
    }
}
